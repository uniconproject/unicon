# reverse() reverses codepoint order, not UTF-8 bytes.

procedure main()
   s := "café"
   r := reverse(s)
   write(*r, " ", r)
   write(reverse(r) == s)
   write(reverse("aébcd"))
   write(reverse("hello"))
end
