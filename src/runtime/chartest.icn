link graphics
procedure main()
   &window := open("mywin","g")
   every n2 := 16r80 to 16rFC do {
      if ((16r80 < n2) % 32 = 0) then WWrite()
      WWrites("\x90" || char(n2))
      }
   Event()
end
