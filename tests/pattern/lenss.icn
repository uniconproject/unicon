procedure main()

   p := Fence() || Len(4) -> yr || " " || Len(4) -> mo || " " || Len(2) -> day
   in := open("leninput", "r")
   out := open("lenoutput", "w")

   every write(out, (line := !in) ?? p := mo || " " || day || ", " || yr || " " )
#	while line := read(in) do
#	{	

#        line ?? p := mo || " " || day || ", " || yr || " " 

#        write(out,line)
#		write(" mo = ", mo, " day = ",day, " yr = ",yr)
#	}
end

