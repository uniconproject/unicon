procedure main()
   out := &output
   s := "abcda"
   if s ?? PPos(1) && "b" then
      write("Match succeeded")
   else
      write("Match failed")
   s ?? PLen(3) -> out && PRpos(0) 
   s ?? PPos(4) && PLen(1) -> out
   if s ?? PPos(1) && "abcd" && PRpos(0) then
      write("Match succeeded")
   else
      write("Match failed")   out := &output
   s := "abcda"
   s ?? PLen(3) -> out
   s ?? PLen(2) -> out && "a"
end
