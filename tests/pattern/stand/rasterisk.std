Asterisk Concatenation with csets
pattern_9(7) = Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: abcd
The matched string is "abcd" the remainder is ""


Asterisk Concatenation with csets with Any('a')
pattern_25(8) = Any('a') || Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: abcd
The matched string is "abcd" the remainder is ""


Asterisk Concatenation with strings
pattern_39(7) = Arbno("aa") || Arbno("bb") || Arbno("cc") || Any('d')
The string is: abcd
The matched string is "d" the remainder is ""


Nested Asterisks
pattern_61(15) = Arbno(("abc" .| (Any('a') || Arbno(Any('b')) || Any('c') .| Any('a') || Any('b') || Arbno(Any('c'))))) || Any('d')
The string is: abcd
The matched string is "abcd" the remainder is ""


Asterisk Concatenation with csets
pattern_75(7) = Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: bbccdd
The matched string is "bbccd" the remainder is "d"


Asterisk Concatenation with csets with Any('a')
pattern_91(8) = Any('a') || Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: bbccdd
The regex failed to match


Asterisk Concatenation with strings
pattern_105(7) = Arbno("aa") || Arbno("bb") || Arbno("cc") || Any('d')
The string is: bbccdd
The matched string is "bbccd" the remainder is "d"


Nested Asterisks
pattern_127(15) = Arbno(("abc" .| (Any('a') || Arbno(Any('b')) || Any('c') .| Any('a') || Any('b') || Arbno(Any('c'))))) || Any('d')
The string is: bbccdd
The matched string is "d" the remainder is "d"


Asterisk Concatenation with csets
pattern_141(7) = Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: aaaabbbbccccd
The matched string is "aaaabbbbccccd" the remainder is ""


Asterisk Concatenation with csets with Any('a')
pattern_157(8) = Any('a') || Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: aaaabbbbccccd
The matched string is "aaaabbbbccccd" the remainder is ""


Asterisk Concatenation with strings
pattern_171(7) = Arbno("aa") || Arbno("bb") || Arbno("cc") || Any('d')
The string is: aaaabbbbccccd
The matched string is "aaaabbbbccccd" the remainder is ""


Nested Asterisks
pattern_193(15) = Arbno(("abc" .| (Any('a') || Arbno(Any('b')) || Any('c') .| Any('a') || Any('b') || Arbno(Any('c'))))) || Any('d')
The string is: aaaabbbbccccd
The matched string is "d" the remainder is ""


Asterisk Concatenation with csets
pattern_207(7) = Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: abcabbbbcabccccd
The matched string is "abccccd" the remainder is ""


Asterisk Concatenation with csets with Any('a')
pattern_223(8) = Any('a') || Arbno(Any('a')) || Arbno(Any('b')) || Arbno(Any('c')) || Any('d')
The string is: abcabbbbcabccccd
The matched string is "abccccd" the remainder is ""


Asterisk Concatenation with strings
pattern_237(7) = Arbno("aa") || Arbno("bb") || Arbno("cc") || Any('d')
The string is: abcabbbbcabccccd
The matched string is "ccccd" the remainder is ""


Nested Asterisks
pattern_259(15) = Arbno(("abc" .| (Any('a') || Arbno(Any('b')) || Any('c') .| Any('a') || Any('b') || Arbno(Any('c'))))) || Any('d')
The string is: abcabbbbcabccccd
The matched string is "abcabbbbcabccccd" the remainder is ""


