#
# main() procedure for Unicon compiler.  In a separate module so that the
# compiler can be linked in as a procedure within larger programs such
# as IDEs.
#
procedure main(argv)
    exit(unicon(argv))
end
