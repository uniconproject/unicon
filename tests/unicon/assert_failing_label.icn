
$define __debug__

# Greptile: label expression must not suppress assertion diagnostics if it fails.
procedure label_always_fails()
   fail
end

procedure main()
   assert(1 = 2, label_always_fails())
   write("should not reach here")
end
