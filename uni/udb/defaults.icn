#
# defaults.icn
# March 16, 2007
#


#
# the debug class variables State & RunCode will have values from those
# State   := (NONE|RUN|GUIT|PAUSE|ERROR|END)
# RunCode := (RUN|CONTINUE|STEP|NEXT)
#
$define NONE     0  # No State value, No Program loaded, "./udb" with no TP
$define QUIT     1
$define CONTINUE 2
$define RUN      3  # run: break, run, help, quit.
$define STEP     4  # step: step.
$define NEXT     5  # next: next.
$define PAUSE    6  # pause: continue, restart, break,
                    # pause: step, print, where, quit, help, next.
$define ERROR    7  # error: where, print, restart, help, quit.
$define END      8  # end: restart, help, quit
$define LOAD     9  # for loading the tp and initializing the debugger
$define RERUN    10 # for the re-running (restarting) state
$define SKIP     11 # To skip some actions

##
## Error Number codes, we may have a table of them next time!
##
##$define Error_0  0  # Zerro Error found
##$define Error_1  1  # Run-Time Error
##$define Error_2  2  # TP File Not Loaded
##$define Error_3  3  # TP is not Accessable
##$define Error_4  4  # Invalid Command
##$define Error_5  5  # if you typed run and no program is pre-loaded
##$define Error_6  6  # when the program exit normally by the end of debugging
##$define Error_7  7  # evalTree can not be printed unless the TP ran for a while.
##$define Error_8  8  # trying to continue a finished program
##$define Error_9  9  # No program is running to execute such cmd
##$define Error_10 10 # The program is successfully loaded
##$define Error_11 11 # No program is loaded neither is running to set break points

#
# The UDB PROMPT shape!!!
#
$define PROMPT "(udb) "


#
# The UDB Version
#
$define VERSION "\t Version 1.0, May 2007."


#
# Set of all the letters and all digits
#
$define CHARACTER &letters++&digits


