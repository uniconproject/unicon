# No __debug__ / __test__: assert/assert_not must preprocess away entirely
# (see ASSERT.md). This program should compile and run with only the write().

procedure main()
   local x
   x := 1
   assert(x = 1)
   assert_not(x = 2)
   write("strip-ok")
end
