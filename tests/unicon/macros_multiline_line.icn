# Runtime &line must match the first physical line of a multiline macro call
# (see preproc_macro_arg_next_line in preproce.icn).
$define ASLINE(x) x

procedure main()
   write(&line)
   write(ASLINE(&line
   ))
   write(&line)
end
