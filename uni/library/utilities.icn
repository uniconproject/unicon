#PD:
#: get_active_name(proc_or_obj) - this will get the name of the current active
#: object or procedure
#:
procedure get_active_name(proc_or_obj)
    return ::image(proc_or_obj) ? {="procedure "; ::tab(0) }
end
