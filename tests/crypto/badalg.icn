#
# Unknown digest name fails open "eh" with error 1311.
#

procedure main()
   # open() fails; &errornumber is the unknown-algorithm code.
   if open("notadigest", "eh") then
      write("badalg:fail")
   else
      write("badalg:ok ", &errornumber)
end
