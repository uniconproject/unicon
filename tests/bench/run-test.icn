############################################################################
#
#	File:     run-test.icn
#
#	Subject:  Run Unicon Benchmarks with test n
#
#	Author:   Shea Newton
#
#	Date:     January 3, 2014
#
############################################################################
#
#   This file is in the public domain.
#
############################################################################
#
#   Compile and run without arguments to run entire suite or
#   specify as runtime arguments which benchmarks to run.
#
############################################################################

link "auxiliary" 

$ifdef _UNIX
$define NUL "/dev/null"
$else
   $define NUL "\\nul"
$endif

global output

procedure main(argv)
   local rL, tL, concurrent, tests := []

   concurrent := verify_concurrent()
   
   output := open(NUL, "w") | stop("Cannot open " || NUL || " for writing")

   if *argv < 1 then {
      if /concurrent then
         write_error(3)
      put(tests, "all")
      }
   else every put(tests, !argv) 
   while test := get(tests) do {
      case test of {
         "all" : {

            write_header()

            writes("concord concord.test          ")
            tL := gettimes(run_concord, "concord.test")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("deal 500                      ")
            tL := gettimes(run_deal, 500)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("ipxref ipxref.test            ")
            tL := gettimes(run_ipxref, "ipxref.test")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("queens 9                      ")
            tL := gettimes(run_queens, 9)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("rsg rsg.test                  ")
            tL := gettimes(run_rsg, "rsg.test")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            
            if \concurrent then {
	       writes("binary-trees 10               ")
               tL := gettimes(run_binarytrees, 10)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               }
            
            if \concurrent then {
	       writes("chameneos-redux 600           ")
               tL := gettimes(run_chameneos, 600)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               }

            writes("fannkuch 7                    ")
            tL := gettimes(run_fannkuch, 7)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("fasta 1000                    ")
            tL := gettimes(run_fasta, 1000)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("k-nucleotide 25-thou.dat      ")
            tL := gettimes(run_knucleotide, "25-thou.dat")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            if \concurrent then {
	       writes("mandelbrot 200                ")
               tL := gettimes(run_mandelbrot, 200)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               }

            writes("meteor-contest 1              ")
            tL := gettimes(run_meteorcontest, 1)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("n-body 1000                   ")
            tL := gettimes(run_nbody, 1000)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("pidigits 27                   ")
            tL := gettimes(run_pidigits, 27)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            if \concurrent then {
	       writes("regex-dna 10-thou.dat         ")
               tL := gettimes(run_regexdna, "10-thou.dat")
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               }

            writes("reverse-complement 1-thou.dat ")
            tL := gettimes(run_reversecomplement, "1-thou.dat")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            writes("spectral-norm 100             ")
            tL := gettimes(run_spectralnorm, 100)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])

            if \concurrent then {
	       writes("thread-ring 1000              ")
               tL := gettimes(run_threadring, 1000)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               }
            
            }
         "concord" : {
            write_header()
            writes("concord concord.test           ")
            tL := gettimes(run_concord, "concord.test")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "deal" : {
            write_header()
            writes("deal 500                      ")
            tL := gettimes(run_deal, 500)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "ipxref" : {
            write_header()
            writes("ipxref ipxref.test            ")
            tL := gettimes(run_ipxref, "ipxref.test")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "queens" : {
            write_header()
            writes("queens 9                       ")
            tL := gettimes(run_queens, 9)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "rsg" : {
            write_header()
            writes("rsg rsg.test                   ")
            tL := gettimes(run_rsg, "rsg.test")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "binary-trees" : {
            write_header()
            writes("binary-trees 10                ")
            if \concurrent then {
               tL := gettimes(run_binarytrees, 10)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               } else write_error(2)
            }
         "chameneos-redux" : {
            write_header()
            writes("chameneos-redux 600            ")
            if \concurrent then {
               tL := gettimes(run_chameneos, 600)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               } else write_error(2)
            }
         "fannkuch" : {
            write_header()
            writes("fannkuch 7                     ")
            tL := gettimes(run_fannkuch, 7)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "fasta" : {
            write_header()
            writes("fasta 1000                     ")
            tL := gettimes(run_fasta, 1000)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "k-nucleotide": {
            write_header()
            writes("k-nucleotide 25-thou.dat        ")
            tL := gettimes(run_knucleotide, "25-thou.dat")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "mandelbrot" : {
            write_header()
            writes("mandelbrot 200                  ")
            if \concurrent then {
               tL := gettimes(run_mandelbrot, 200)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               } else write_error(2)
            }
         "meteor-contest" : {
            write_header()
            writes("meteor-contest 1                ") 
            tL := gettimes(run_meteorcontest, 1)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "n-body" : {
            write_header()
            writes("n-body 1000                     ")
            tL := gettimes(run_nbody, 1000)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "pidigits" : {
            write_header()
            writes("pidigits 27                     ")
            tL := gettimes(run_pidigits, 27)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "regex-dna" : {
            write_header()
            writes("regex-dna 10-thou.dat           ")
            if \concurrent then {
               tL := gettimes(run_regexdna, "10-thou.dat")
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               } else write_error(2)
            }
         "reverse-complement" : {
            write_header()
            writes("reverse-complement 1-thou.dat  ")
            tL := gettimes(run_reversecomplement, "1-thou.dat")
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "spectral-norm" : {
            write_header()
            writes("spectral-norm 100              ")
            tL := gettimes(run_spectralnorm, 100)
            rL := calctimes(tL[1], tL[2], tL[3], tL[4])
            writetime(rL[1], rL[2], rL[3])
            }
         "thread-ring" : {
            write_header()
            writes("thread-ring 1000               ")
            if \concurrent then {
               tL := gettimes(run_threadring, 1000)
               rL := calctimes(tL[1], tL[2], tL[3], tL[4])
               writetime(rL[1], rL[2], rL[3])
               } else write_error(2)
            }
         default : write("\ncannot run benchmark " || test || "\n")
         }
      }
end
