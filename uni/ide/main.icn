procedure main(argv)
   local d
   ui_initialize()
   d := dialog()
   # load file from command line argument, if any; add options etc.
   if *argv>0 then
      current_file := defextension(argv[1])
   d.show_modal()
end
