import ulsp

procedure main()
    database := LSPDB()
    database.build()
    print_table(database.package_db)
end

procedure print_table(t)
    if (string(!t)) then
        every entry := !t do
            write(entry)
    else 
        every print_table(!t)
end