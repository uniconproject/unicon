procedure main()

	p := PFence() && PLen(4) -> yr && " " && PLen(4) -> mo && " " && PLen(2) -> day



	in := open("leninput", "r")
	out := open("lenoutput", "w")
	
#	every	write(out, (pattern_match(!(in),p) := mo || " " || day || ", " || yr || " " ))

	every	write(out, (line := !in) ?? p := mo || " " || day || ", " || yr || " " )
#	while line := read(in) do
#	{	

#        line ?? p := mo || " " || day || ", " || yr || " " 

#        write(out,line)
#		write(" mo = ", mo, " day = ",day, " yr = ",yr)
#	}
end

