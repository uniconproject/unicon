procedure main()
    p := ( "b" .| "r") && ("e" .|"ea") && ("d" .| "ds")
    pattern := .$x && p && .$y
    write("the beads are red")
    if "the beads are red" ?? pattern then 
      write(repl(" ",x - 1) , repl("_", y - x))
end
