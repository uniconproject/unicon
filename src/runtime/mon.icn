$include "evdefs.icn"
link evinit
procedure main(argv)
    display(,,&main)
    write(image(proc(&main)) | " &main failed")
#    EvInit(argv)
#    while EvGet() do {
#	writes("eventcode ", &eventcode, " eventvalue ", image(&eventvalue))
#	writes(" line ",keyword("line", Monitored)) | writes(" no line ")
#	writes(" file ",keyword("file", Monitored)) | writes(" no file ")
#	write()
#	write("proc: " || image(proc(Monitored)) | "proc() failed")
#    }
end
